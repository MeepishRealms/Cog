name: Build Snowstorm
on: 
  push:
    paths: 'snowstorm/version.json'
  release:
    types: [published]
  workflow_dispatch:
jobs:
  compress-archive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: TheDoctor0/zip-release@0.7.6
        with:
          type: zip
          filename: snowstorm_autobuild.zip
          path: snowstorm
      - uses: actions/upload-artifact@v2
        with:
          name: snowstorm
          path: ./snowstorm_autobuild.zip
