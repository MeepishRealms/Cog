name: Build .mrpack
on: 
  release:
    types: [published]
  workflow_dispatch:
jobs:
  tar-snowstorm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: TheDoctor0/zip-release@0.7.6
        with:
          type: zip
          filename: cogno_autobuild.mrpack
          path: mrpack
      - uses: actions/upload-artifact@v2
        with:
          name: modrinth
          path: ./cogno_autobuild.mrpack
